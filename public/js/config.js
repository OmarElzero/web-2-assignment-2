const APP_CONFIG = {
	// For local development this can stay as `${window.location.origin}/api`.
	// For production, set it to your backend URL + `/api`.
	API_BASE_URL: `${window.location.origin}/api`,
};
